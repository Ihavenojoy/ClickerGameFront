{
    "version": "0.2.0",
    "configurations": [
        {
            "comment": "install of all dependancy's, requered for first boot up",
            "command": "npm install",
            "name": "Run npm install",
            "request": "launch",
            "type": "node-terminal"
        },
        {
            "comment": "start application in dev mode (extra tools and all pages navigatable + bonus debug information",
            "command": "npm run dev",
            "name": "Run run dev",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}